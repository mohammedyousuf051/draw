version: '3.8'

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: test_lucky_draw
    ports:
      - "5433:5432"